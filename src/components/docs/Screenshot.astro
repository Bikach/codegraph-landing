---
interface Props {
  src?: string;
  alt: string;
  caption?: string;
}

const { src, alt, caption } = Astro.props;
---

<figure class="my-6">
  {src ? (
    <img
      src={src}
      alt={alt}
      class="w-full rounded-lg border border-border shadow-sm transition-transform duration-200 hover:scale-[1.02]"
    />
  ) : (
    <div class="flex h-64 w-full items-center justify-center rounded-lg border border-border bg-bg-subtle shadow-sm transition-transform duration-200 hover:scale-[1.02]">
      <span class="text-text-muted">Screenshot coming soon</span>
    </div>
  )}
  {caption && (
    <figcaption class="mt-2 text-center text-sm text-text-muted">
      {caption}
    </figcaption>
  )}
</figure>
