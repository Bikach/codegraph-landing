---
// QuickStart section - Get Started in Minutes
---

<section id="quick-start" class="relative py-24 lg:py-32">

  <div class="relative mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <!-- Section header -->
    <div class="text-center mb-12 lg:mb-16">
      <h2 class="text-3xl font-bold tracking-tight sm:text-4xl lg:text-5xl mb-4">
        <span class="gradient-text">Get Started in Minutes</span>
      </h2>
      <p class="text-lg text-[var(--color-text-secondary)] max-w-2xl mx-auto">
        Choose your preferred installation method and start navigating your codebase intelligently
      </p>
    </div>

    <!-- Installation Options Grid -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 lg:gap-8 mb-12">

      <!-- Option A: Claude Code Plugin (Recommended) -->
      <div class="glass-card p-6 lg:p-8 relative overflow-hidden">
        <!-- Header -->
        <div class="flex items-start justify-between gap-3 mb-4">
          <div class="flex items-center gap-3">
            <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-[var(--color-primary)]/10 flex-shrink-0">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-[var(--color-primary)]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" />
              </svg>
            </div>
            <div>
              <h3 class="text-xl font-semibold text-[var(--color-text-primary)]">Claude Code Plugin</h3>
              <p class="text-sm text-[var(--color-text-secondary)]">The fastest way to get started</p>
            </div>
          </div>
          <!-- Recommended Badge -->
          <span class="inline-flex items-center gap-1 px-2 py-1 rounded-full text-[10px] sm:text-xs font-semibold bg-[var(--color-secondary)] text-white flex-shrink-0 whitespace-nowrap">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 sm:h-3.5 sm:w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
            </svg>
            Recommended
          </span>
        </div>

        <!-- Highlight -->
        <div class="mb-6 p-3 rounded-lg bg-[var(--color-primary)]/10 border border-[var(--color-primary)]/20">
          <p class="text-sm text-[var(--color-primary)] flex items-center gap-2">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            Just 4 commands to get started
          </p>
        </div>

        <!-- Code Block -->
        <div class="code-block relative">
          <div class="code-header px-4 py-2 bg-[var(--color-bg-subtle)] border-b border-[var(--color-border)] rounded-t-lg">
            <span class="text-xs text-[var(--color-text-muted)] font-mono">bash</span>
          </div>
          <div class="code-content bg-[var(--color-bg-subtle)] rounded-b-lg p-4 overflow-x-auto border-x border-b border-[var(--color-border)]">
            <pre class="text-sm leading-relaxed"><code><span class="step-line"><span class="step-number">1</span><span class="step-command">/plugin marketplace add Bikach/claude-plugins</span></span>
<span class="step-line"><span class="step-number">2</span><span class="step-command">/plugin install codegraph@Bikach</span></span>
<span class="step-line"><span class="step-number">3</span><span class="step-command">/codegraph:setup</span></span>
<span class="step-line"><span class="step-number">4</span><span class="step-command">/codegraph:index</span></span></code></pre>
          </div>
        </div>

        <!-- Footer note -->
        <p class="mt-4 text-sm text-[var(--color-text-muted)]">
          Automatic Neo4j setup, no configuration needed
        </p>
      </div>

      <!-- Option B: Standalone MCP -->
      <div class="glass-card p-6 lg:p-8 relative">
        <!-- Header -->
        <div class="flex items-start justify-between gap-3 mb-4">
          <div class="flex items-center gap-3">
            <div class="flex items-center justify-center w-10 h-10 rounded-lg bg-[var(--color-border)] flex-shrink-0">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-[var(--color-text-secondary)]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M5.25 14.25h13.5m-13.5 0a3 3 0 01-3-3m3 3a3 3 0 100 6h13.5a3 3 0 100-6m-16.5-3a3 3 0 013-3h13.5a3 3 0 013 3m-19.5 0a4.5 4.5 0 01.9-2.7L5.737 5.1a3.375 3.375 0 012.7-1.35h7.126c1.062 0 2.062.5 2.7 1.35l2.587 3.45a4.5 4.5 0 01.9 2.7m0 0a3 3 0 01-3 3m0 3h.008v.008h-.008v-.008zm0-6h.008v.008h-.008v-.008zm-3 6h.008v.008h-.008v-.008zm0-6h.008v.008h-.008v-.008z" />
              </svg>
            </div>
            <div>
              <h3 class="text-xl font-semibold text-[var(--color-text-primary)]">Standalone MCP</h3>
              <p class="text-sm text-[var(--color-text-secondary)]">For custom setups and integrations</p>
            </div>
          </div>
        </div>

        <!-- Spacer to align with recommended card -->
        <div class="mb-6 h-[52px]"></div>

        <!-- Code Block -->
        <div class="code-block relative">
          <div class="code-header px-4 py-2 bg-[var(--color-bg-subtle)] border-b border-[var(--color-border)] rounded-t-lg">
            <span class="text-xs text-[var(--color-text-muted)] font-mono">bash</span>
          </div>
          <div class="code-content bg-[var(--color-bg-subtle)] rounded-b-lg p-4 overflow-x-auto border-x border-b border-[var(--color-border)]">
            <pre class="text-sm leading-relaxed"><code><span class="step-line"><span class="step-number">1</span><span class="step-command">docker-compose up -d</span></span>
<span class="step-line"><span class="step-number">2</span><span class="step-command">cd mcp-server && npm install && npm run build</span></span>
<span class="step-line"><span class="step-number">3</span><span class="step-comment"># Configure in .mcp.json</span></span></code></pre>
          </div>
        </div>

        <!-- Footer note -->
        <p class="mt-4 text-sm text-[var(--color-text-muted)]">
          Full control over Neo4j and MCP server configuration
        </p>
      </div>
    </div>

    <!-- CTA -->
    <div class="text-center">
      <a href="/docs/getting-started" class="btn-primary inline-flex items-center gap-2 text-lg px-8 py-4">
        <span>Read the full documentation</span>
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .code-block {
    border: 1px solid var(--color-border);
    border-radius: 8px;
    overflow: hidden;
  }

  .code-content pre {
    margin: 0;
    background: transparent;
    border: none;
    padding: 0;
  }

  .code-content code {
    display: block;
    font-family: 'JetBrains Mono', 'Fira Code', 'SF Mono', Consolas, monospace;
    color: var(--color-text-primary);
    background: transparent;
    padding: 0;
    font-size: 0.875rem;
  }

  .step-line {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.25rem 0;
  }

  .step-number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 1.5rem;
    height: 1.5rem;
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-primary);
    background: rgba(59, 130, 246, 0.15);
    border-radius: 4px;
  }

  .step-command {
    color: var(--color-text-primary);
  }

  .step-comment {
    color: var(--color-text-muted);
    font-style: italic;
  }

  /* Responsive adjustments */
  @media (max-width: 1024px) {
    .code-content {
      padding: 1rem;
    }

    .code-content code {
      font-size: 0.8rem;
    }
  }

  @media (max-width: 640px) {
    .step-line {
      gap: 0.75rem;
    }

    .step-number {
      min-width: 1.25rem;
      height: 1.25rem;
      font-size: 0.65rem;
    }

    .code-content code {
      font-size: 0.75rem;
    }
  }
</style>
